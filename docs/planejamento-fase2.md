**Tarefas Pendentes:**

- **Cobertura:** rodar `mvn clean verify`, abrir `target/site/jacoco/index.html`, escrever os testes que faltarem para bater 80% total (inclusive em código de outros membros, se necessário).
- **Docker Compose:** subir `docker compose up --build`, validar que Flyway roda com as 4 tabelas e a API responde.
- **README:** escrever do zero as seções de arquitetura Clean Architecture, modelo de dados final (4 tabelas), tabela de endpoints completa, seção de grupo, como executar localmente, como verificar os testes... Tudo que é cobrado no desafio.
- **Postman:** valdiar a collection do postman criada
- **Vídeo (~5min):** roteiro sugerido — contexto (30s) → arquitetura em camadas (30s) → demo Postman do fluxo completo (2-3min) → Swagger UI (30s) → `mvn clean verify` com testes verdes e cobertura ≥80% (30s) → encerramento.
